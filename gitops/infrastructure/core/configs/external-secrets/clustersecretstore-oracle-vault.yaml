apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: oracle-vault
spec:
  provider:
    oracle:
      vault: "${OCI_VAULT_ID}"
      region: "${OCI_REGION}"
      principalType: InstancePrincipal
